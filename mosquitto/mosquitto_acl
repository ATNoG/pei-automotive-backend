# Backend microservices — full read/write access on all topics
user backend
topic readwrite #

# Android clients authenticate with their Keycloak JWT; the broker extracts the
# 'sub' claim (a UUID) and uses it as the MQTT username.
pattern read cars/updates
pattern read cars/station/#
pattern read alerts/#
pattern read meteo/updates
